name: Auto Update WCG Stats & Deploy

on:
  schedule:
    - cron: '0 */6 * * *' # 6시간마다 자동 실행 (UTC 기준)
  workflow_dispatch:      # 수동 실행 버튼 (테스트용)

permissions:
  contents: write         # 저장소 수정 및 배포 권한 필수

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. 소스 코드 가져오기
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 2. 파이썬으로 최신 데이터 긁어오기
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Install Python dependencies
        run: |
          pip install beautifulsoup4 requests

      - name: Run Scraping Script
        # ⚠️ [확인] 파이썬 파일이 scripts 폴더 안에 있어야 합니다.
        # 만약 맨 바깥에 있다면 'python fetch_real_data_bs4.py'로 고치세요.
        run: |
          python scripts/fetch_real_data_bs4.py

      # 3. 갱신된 데이터(JSON)를 소스코드(Main)에 저장 (선택 사항)
      # 이걸 해두면 깃허브 코드창에서도 숫자가 바뀐 게 보입니다.
      - name: Commit Updated Data
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          # ⚠️ [확인] 파이썬이 수정하는 파일 경로를 적어주세요. (예: public/data.json)
          git add .
          git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Auto-update WCG stats" && git push)

      # 4. Node.js 설정 및 빌드 (React -> HTML/JS 변환)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # 최신 버전 사용

      - name: Install & Build
        run: |
          npm install       # package.json을 보고 필요한 도구 설치
          npm run build     # ⚡️ 여기서 최신 데이터를 포함해 사이트를 새로 만듭니다!

      # 5. 완성된 사이트(dist)를 gh-pages 브랜치로 배포
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist # Vite의 기본 빌드 결과물 폴더
          force_orphan: true  # 찌꺼기 파일 없이 깔끔하게 덮어쓰기
